import { fr } from '@braid/vue-formulate-i18n'

export default{
    plugins:[fr],
    locale:'fr',
    classes:{
        wrapper:['w-full'],
        input: ['form__input'],
        element:['my-1'],
        errors: ['text-red-400 text-xs px-1'],
        label:[''],
    }
}